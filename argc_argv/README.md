# argc_argv - C Programming

This project introduces **command line arguments** in C.

The goal is to understand how arguments are passed to programs using:

```c
int main(int argc, char *argv[]);
``` id="kdnq6t"

---

## 📌 Learning Objectives

At the end of this project, you should be able to explain:

- What `argc` and `argv` mean
- How to use command line arguments
- How arguments are stored in memory
- Why `argv[argc]` is NULL
- How to access program name and arguments

---

## 📂 Project Structure

